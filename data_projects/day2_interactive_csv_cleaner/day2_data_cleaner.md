This tool loads a CSV file, displays summary statistics, applies rule-based missing value handling depending on data type, and saves a cleaned version to a user defined path.

Design goals:
* Robust input handling
* Clear data transformation rules
* Reusable, interactive workflow

Requirements to run:
* Python => v3.10
* Pandas

Paste this into your terminal to run:
python cleaner.py